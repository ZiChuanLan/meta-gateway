bin/
data/
.env
*.db
*.db-*
web/node_modules/
web/src/node_modules/
web/coverage/

# Local binaries / agent scratch
/server
.pi-subagents/
.tools/
.cursor/
.pi-glla/
.playwright-mcp/
.zcode/
*.exe

# Accidental root lockfile (frontend lock lives under web/)
/package-lock.json

# Tool artifacts (design-skill sessions, external tools)
.workbuddy/
web/.impeccable/
.impeccable/
web/.gitignore
web/shots/

# Debug logs and ad-hoc analysis scripts
*.log
scripts/

# Debug snapshots and verification screenshots dumped at the repo root
# (tracked docs assets live under docs/screenshots and are not affected)
/*.png
/*.txt

# Ad-hoc debug databases and working trees from repro sessions
.tmp-db/
.tmp-repro/

# Generated by the release workflow inside CI; local copies are leftover noise
/release-notes.md

# Stray frontend cache from running vitest at the repo root by mistake
/node_modules/
